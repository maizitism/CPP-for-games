#pragma once // makes sure that if file is included more then once, preprocessor doesnt include it again

// adding variables in global scope is bad practice
float jumpHeight = 50.0f;
float jumpDuration = 0.5f;
// These are define variables - storage is being allocated for these varioables in memory
// Variables will be stored in main.obj file.